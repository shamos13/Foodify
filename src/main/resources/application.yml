server:
  port: 5454

spring:
  application:
    name: Online Food Ordering

  datasource:
    url: "jdbc:mysql://localhost:3306/foodify"
    username: "root"
    password: "Tarak#23h!"                # <- MUST be quoted because of the '#'
    driver-class-name: "com.mysql.cj.jdbc.Driver"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL8Dialect"
