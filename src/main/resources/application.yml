server:
  port: 5454

spring:
  application:
    name: Online Food Ordering

  datasource:
    url: "jdbc:mysql://localhost:3306/foodify"
    username: "root"
    password: "Tarak#23h!"                # <- MUST be quoted because of the '#'

  jpa:
    hibernate:
      ddl-auto: update #if you are using update its better to implement create first then update later
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQLDialect"
